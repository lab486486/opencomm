---
/**
 * Kept for compatibility. Cards are now injected server-side in [...slug].astro
 * via injectInternalLinkCards() so previews work without client JS.
 */
interface Props {
  cards?: Record<string, unknown>;
}
---
